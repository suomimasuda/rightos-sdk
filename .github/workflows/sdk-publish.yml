# Publish @i-s3/rightos (TypeScript SDK) to npm via Trusted Publishing (OIDC only).
# Do NOT create npm granular access tokens for CI — they expire ≤90 days.
# Setup: npm Packages → @i-s3/rightos → Settings → Trusted publishing
#   owner=suomimasuda repo=rightos-sdk workflow=sdk-publish.yml
name: sdk-publish

on:
  workflow_dispatch:
  push:
    tags:
      - "sdk-v*"

permissions:
  id-token: write
  contents: read

jobs:
  release:
    runs-on: ubuntu-latest
    permissions:
      id-token: write
      contents: read
    defaults:
      run:
        working-directory: typescript
    steps:
      - uses: actions/checkout@v4

      - uses: actions/setup-node@v4
        with:
          node-version: 24
          registry-url: https://registry.npmjs.org

      - name: Validate tag matches package.json
        if: startsWith(github.ref, 'refs/tags/sdk-v')
        run: |
          set -eu
          TAG="${GITHUB_REF#refs/tags/}"
          EXPECTED="${TAG#sdk-v}"
          PKG_VER=$(node -p "require('./package.json').version")
          test "$EXPECTED" = "$PKG_VER" || { echo "package.json version mismatch"; exit 1; }

      - name: Build TypeScript SDK
        run: |
          npm install
          npm run build

      - name: OIDC dry-run (workflow_dispatch)
        if: github.event_name == 'workflow_dispatch'
        run: bash ../scripts/oidc-npm-publish.sh dry-run

      - name: Publish to npm (tag)
        if: startsWith(github.ref, 'refs/tags/sdk-v')
        run: bash ../scripts/oidc-npm-publish.sh publish
